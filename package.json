{
	"name": "consortium-kernel",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"db:init": "wrangler d1 execute consortium_ledger --local --file=./schemas/ledger.init.sql",
		"db:init:remote": "wrangler d1 execute consortium_ledger --remote --file=./schemas/ledger.init.sql",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.3.0",
		"ajv": "^8.18.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.66.0"
	}
}
